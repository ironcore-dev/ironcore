# permissions to do viewer subnets.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: subnet-viewer-role
rules:
- apiGroups:
  - core.api.onmetal.de
  resources:
  - subnets
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - core.api.onmetal.de
  resources:
  - subnets/status
  verbs:
  - get
