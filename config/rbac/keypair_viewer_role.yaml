# permissions to do viewer keypairs.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: keypair-viewer-role
rules:
- apiGroups:
  - core.api.onmetal.de
  resources:
  - keypairs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - core.api.onmetal.de
  resources:
  - keypairs/status
  verbs:
  - get
