# permissions to do viewer gateways.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: gateway-viewer-role
rules:
- apiGroups:
  - core.api.onmetal.de
  resources:
  - gateways
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - core.api.onmetal.de
  resources:
  - gateways/status
  verbs:
  - get
