apiVersion: storage.api.onmetal.de/v1alpha1
kind: Volume
metadata:
  name: volume-sample
spec:
  volumeClassRef:
    name: volumeclass-sample
  volumePool:
    name: volumepool-sample
  resources:
    storage: 100Gi
