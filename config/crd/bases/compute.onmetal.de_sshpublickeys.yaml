
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: sshpublickeys.compute.onmetal.de
spec:
  group: compute.onmetal.de
  names:
    kind: SSHPublicKey
    listKind: SSHPublicKeyList
    plural: sshpublickeys
    singular: sshpublickey
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.expirationDate
      name: Expiration
      type: date
    - jsonPath: .status.algorithm
      name: Algorithm
      type: string
    - jsonPath: .status.keyLength
      name: KeyLength
      type: integer
    - jsonPath: .status.state
      name: StateFields
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SSHPublicKey is the Schema for the sshpublickeys API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SSHPublicKeySpec defines the desired state of SSHPublicKey
            properties:
              description:
                description: Description describes the purpose of the ssh key
                type: string
              expirationDate:
                description: ExpirationDate indicates until when this public key is
                  valid
                format: date-time
                type: string
              sshPublicKey:
                description: SSHPublicKey is the SSH public key string
                type: string
            required:
            - sshPublicKey
            type: object
          status:
            description: SSHPublicKeyStatus defines the observed state of SSHPublicKey
            properties:
              algorithm:
                description: Algorithm is the algorithm used to generate the ssh key
                type: string
              fingerPrint:
                description: FingerPrint is the finger print of the ssh public key
                type: string
              keyLength:
                description: KeyLength is the byte length of the ssh key
                minimum: 0
                type: integer
              message:
                description: Message contains a message for the corresponding state
                type: string
              publicKey:
                description: PublicKey is the PEM encoded public key
                type: string
              state:
                description: State indicates the state of a resource
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
