
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.2
  creationTimestamp: null
  name: sshpublickeys.compute.onmetal.de
spec:
  group: compute.onmetal.de
  names:
    kind: SSHPublicKey
    listKind: SSHPublicKeyList
    plural: sshpublickeys
    singular: sshpublickey
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.expirationDate
      name: Expiration
      type: date
    - jsonPath: .status.algorithm
      name: Algorithm
      type: string
    - jsonPath: .status.keyLength
      name: KeyLength
      type: integer
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SSHPublicKey is the Schema for the sshpublickeys API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SSHPublicKeySpec defines the desired state of SSHPublicKey
            properties:
              configMapRef:
                description: ConfigMapRef is the reference to a key in a ConfigMap
                  resource containing the public key data. If key is not specified,
                  it defaults to 'id_rsa.pub'.
                properties:
                  key:
                    description: The key of the entry in the ConfigMap resource's
                      `data` field to be used. Some instances of this field may be
                      defaulted, in others it may be required.
                    type: string
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
            required:
            - configMapRef
            type: object
          status:
            description: SSHPublicKeyStatus defines the observed state of SSHPublicKey
            properties:
              algorithm:
                description: Algorithm is the algorithm used for the public key.
                type: string
              conditions:
                description: Conditions are a list of elements indicating the status
                  of various properties of an SSHPublicKey.
                items:
                  description: SSHPublicKeyCondition is a condition of an SSHPublicKey.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the status
                        of a condition has transitioned from one state to another.
                      format: date-time
                      type: string
                    lastUpdateTime:
                      description: LastUpdateTime is the last time a condition has
                        been updated.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human-readable explanation of why
                        the condition has a certain reason / state.
                      type: string
                    observedGeneration:
                      description: ObservedGeneration represents the .metadata.generation
                        that the condition was set based upon.
                      format: int64
                      type: integer
                    reason:
                      description: Reason is a machine-readable indication of why
                        the condition is in a certain state.
                      type: string
                    status:
                      description: Status is the corev1.ConditionStatus of the condition.
                      type: string
                    type:
                      description: Type is the SSHPublicKeyConditionType of the condition.
                      type: string
                  required:
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              fingerprint:
                description: Fingerprint is the fingerprint of the ssh public key.
                type: string
              keyLength:
                description: KeyLength is the byte length of the ssh key.
                minimum: 0
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
