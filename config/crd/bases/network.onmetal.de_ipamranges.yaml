
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.2
  creationTimestamp: null
  name: ipamranges.network.onmetal.de
spec:
  group: network.onmetal.de
  names:
    kind: IPAMRange
    listKind: IPAMRangeList
    plural: ipamranges
    shortNames:
    - ipr
    singular: ipamrange
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.cidrs
      name: CIDRS
      type: string
    - jsonPath: .status.cidrs
      name: EffectiveCIDRs
      type: string
    - jsonPath: .spec.parent.name
      name: Parent
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IPAMRange is the Schema for the ipamranges API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: IPAMRangeSpec defines the desired state of IPAMRange Either
              parent and size or a give CIDR must be specified. If parent is specified,
              the effective range of the given size is allocated from the parent IP
              range. If parent and CIDR is defined, the given CIDR must be in the
              parent range and unused. It will be allocated if possible. Otherwise,
              the status of the object will be set to "Invalid".
            properties:
              cidrs:
                description: CIDRs is a list of CIDR specs which are defined for this
                  IPAMRange
                items:
                  type: string
                type: array
              mode:
                description: Mode is the mode to request an IPAMRange.
                type: string
              parent:
                description: Parent is the reference of the Parent IPAMRange from
                  which the Cidr or size should be derived
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
            type: object
          status:
            description: IPAMRangeStatus defines the observed state of IPAMRange
            properties:
              allocationState:
                items:
                  type: string
                type: array
              cidrs:
                description: CIDRs is a list of effective cidrs which belong to this
                  IPAMRange
                items:
                  description: CIDRAllocationStatus is the result of a CIDR allocation
                    request
                  properties:
                    cidr:
                      type: string
                    message:
                      type: string
                    request:
                      type: string
                    status:
                      description: AllocationState is a state an allocation can be
                        in.
                      type: string
                  required:
                  - cidr
                  - request
                  - status
                  type: object
                type: array
              conditions:
                items:
                  description: IPAMRangeCondition is one of the conditions of a volume.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the status
                        of a condition has transitioned from one state to another.
                      format: date-time
                      type: string
                    lastUpdateTime:
                      description: LastUpdateTime is the last time a condition has
                        been updated.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human-readable explanation of why
                        the condition has a certain reason / state.
                      type: string
                    observedGeneration:
                      description: ObservedGeneration represents the .metadata.generation
                        that the condition was set based upon.
                      format: int64
                      type: integer
                    reason:
                      description: Reason is a machine-readable indication of why
                        the condition is in a certain state.
                      type: string
                    status:
                      description: Status is the status of the condition.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  required:
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              message:
                type: string
              pendingDeletions:
                items:
                  description: CIDRAllocationStatus is the result of a CIDR allocation
                    request
                  properties:
                    cidr:
                      type: string
                    message:
                      type: string
                    request:
                      type: string
                    status:
                      description: AllocationState is a state an allocation can be
                        in.
                      type: string
                  required:
                  - cidr
                  - request
                  - status
                  type: object
                type: array
              pendingRequests:
                properties:
                  cidrs:
                    items:
                      properties:
                        cidr:
                          type: string
                        request:
                          type: string
                      required:
                      - cidr
                      - request
                      type: object
                    type: array
                  deletions:
                    items:
                      properties:
                        cidr:
                          type: string
                        request:
                          type: string
                      required:
                      - cidr
                      - request
                      type: object
                    type: array
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
              roundRobinState:
                items:
                  type: string
                type: array
              state:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
