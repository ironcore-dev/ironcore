
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: scopes.core.onmetal.de
spec:
  group: core.onmetal.de
  names:
    kind: Scope
    listKind: ScopeList
    plural: scopes
    singular: scope
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.namespace
      name: Namespace
      type: string
    - jsonPath: .status.account
      name: Account
      type: string
    - jsonPath: .status.parentScope
      name: ParentScope
      type: string
    - jsonPath: .status.parentNamespace
      name: ParentNamespace
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Scope is the Schema for the scopes API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ScopeSpec defines the desired state of Scope
            properties:
              description:
                description: Description is a human-readable description of what the
                  scope is used for.
                type: string
            type: object
          status:
            description: ScopeStatus defines the observed state of Scope
            properties:
              account:
                description: Account descibes the account this scope belongs to
                type: string
              namespace:
                description: Namespace references the namespace of the scope
                type: string
              parentNamespace:
                description: ParentNamespace represents the namespace of the parent
                  scope
                type: string
              parentScope:
                description: ParentScope describes the parent scope, if empty the
                  account should be used as the top reference
                type: string
              state:
                description: State represents the state of the scope
                type: string
            required:
            - account
            - parentNamespace
            - parentScope
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
